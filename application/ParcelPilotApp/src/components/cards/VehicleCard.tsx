import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const VehicleCard = () => {
  return (
    <View style={styles.container}>
      <Text>VehicleCard</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 8 },
});
