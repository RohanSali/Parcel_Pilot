import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const QuickActionCard = () => {
  return (
    <View style={styles.container}>
      <Text>QuickActionCard</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 8 },
});
