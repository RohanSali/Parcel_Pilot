import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const AnalyticsCard = () => {
  return (
    <View style={styles.container}>
      <Text>AnalyticsCard</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 8 },
});
