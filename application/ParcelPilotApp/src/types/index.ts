export interface User {
  id: string;
  name: string;
}

export interface Network {
  id: string;
  name: string;
}

export interface Vehicle {
  id: string;
  state: string;
}

export interface Task {
  id: string;
  status: string;
}
